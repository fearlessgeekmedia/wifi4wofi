{
  "name": "wifi4wofi",
  "version": "2.0.0",
  "description": "A WiFi menu for Wayland using wofi and NetworkManager",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "node-networkmanager": "^1.0.0",
    "node-wofi": "^1.0.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.0.0"
  }
}
